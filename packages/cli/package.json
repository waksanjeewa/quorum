{
  "name": "quorum",
  "version": "0.6.0",
  "description": "Local-first tool where multiple AI models collaborate on one goal — plan and build together, with usage-limit-aware handoff.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "quorum": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "keywords": ["ai", "llm", "agents", "orchestration", "claude", "codex", "local-first"],
  "repository": {
    "type": "git",
    "url": "https://github.com/waksanjeewa/quorum.git"
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@quorum/daemon": "workspace:*",
    "@quorum/dashboard": "workspace:*",
    "@quorum/core": "workspace:*"
  },
  "devDependencies": {
    "@quorum/adapters": "workspace:*",
    "vitest": "^2.1.8",
    "yaml": "^2.6.1"
  }
}
