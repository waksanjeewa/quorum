{
  "name": "@quorum/dashboard",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo \"[dashboard] build placeholder — see task 100\" && mkdir -p dist"
  }
}
